global:
  scrape_interval: 15s  # Frequency of metric scraping

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['prometheus.localhost']

# scrape_configs:
#   - job_name: 'application'  # Descriptive name for the service
#     static_configs:
#       - targets: ['application:3000']  # Replace with container name or IP and port
